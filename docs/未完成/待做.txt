IE浏览器，模态窗口下，如果重新登陆并继续之前的操作，会变成再弹出一个窗口
初始化解析模块配置文件时，保存的顺序不是按配置文件的顺序
js检查输入的时候，显示结果也像后台检查那样
地区管理，检查是否有子节点，用了isEmpty，如果数据多了，可能会影响速度，关于图片的切换也可能会影响速度
由于检查权限用了缓存，所以即使修改了权限，用户使用时权限也不会变，必须清理缓存之后才会改变，可以考虑不用缓存，而是放入session
Authority表增加.moduleType、moduleId、functionId组合索引，增快检查权限的查询速度
每次修改菜单之后，都要重新初始化系统数据，太麻烦，实际运行之后也会出问题
WebUtils.getModule把application改为缓存